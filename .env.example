# Vercel KV (Upstash Redis) — set these in your Vercel project for production.
# Locally you can leave them blank; the app falls back to a local file store (.data/).
KV_REST_API_URL=
KV_REST_API_TOKEN=
KV_REST_API_READ_ONLY_TOKEN=
KV_URL=
